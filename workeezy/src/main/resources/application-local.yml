spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: ${SPRING_DATASOURCE_URL}
    username: ${SPRING_DATASOURCE_USERNAME}
    password: ${SPRING_DATASOURCE_PASSWORD}
#    url: jdbc:mysql://workeezy.iptime.org:3306/workeezy?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
#    username: ${DB_USERNAME}
#    password: ${DB_PASSWORD}

  security:
    debug: true
  data:
    redis:
      host: localhost
      port: 6379
  elasticsearch:
    uris: http://localhost:9200

  jackson:
    time-zone: Asia/Seoul

  jpa:
    hibernate:
      ddl-auto: update  # create, update, validate, none 중 선택
    show-sql: false  # SQL 쿼리 콘솔 출력
    properties:
      hibernate:
        format_sql: true  # SQL 포맷팅
        dialect: org.hibernate.dialect.MySQLDialect
    open-in-view: false  # OSIV 비활성화 (선택)

app:
  cookie:
    secure: false
    same-site: Lax
    domain:

logging:
  level:
#    root: DEBUG
#    org.hibernate.SQL: debug  # SQL 로깅
    org.hibernate.type.descriptor.sql.BasicBinder: trace  # 파라미터 값 로깅
#    org.springframework.security: DEBUG

jwt:
  secret: VEhJU19JU19BX1NVUEVSX0xPTkdfU0VDUkVUX0tFWV9GT1JfSldUX1RPS0VOXzEyMzQ1Njc4
  access-expiration-ms: 1800000     # 30분
  refresh-expiration-ms: 604800000  # 7일

toss:
  payments:
    secret-key: test_gsk_docs_OaPz8L5KdmQXkzRz3y47BMw6
    client-key: test_gck_docs_Ovk5rk1EwkEbP0W43n07xlzm # 프론트에서 사용
    base-url: https://api.tosspayments.com

cloud:
  aws:
    region:
      static: ap-northeast-2
      auto: false
    stack:
      auto: false
    s3:
      bucket: workeezy
      auto: false

draft:
  max-count: 5
  ttl-days: 14